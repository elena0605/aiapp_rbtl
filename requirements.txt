# GraphRAG Project Dependencies
# This file should be placed in the new graphrag/ project root

# Neo4j Database Driver
# neo4j>=5.7

# Langfuse - LLM Observability & Prompt Management
langfuse>=3.0.0

# OpenAI SDK (for standard OpenAI API)
openai>=1.0.0

# OR use Azure OpenAI (choose one):
# azure-openai>=1.0.0

# Environment Variables
python-dotenv>=1.0.0

# YAML Parsing (for prompts, terminology, examples)
pyyaml>=6.0

# Vector operations for similarity search
numpy>=1.24.0

# Optional: For type hints (recommended)
typing-extensions>=4.0.0

# MCP (Model Context Protocol) Client SDK
# Install with: pip install mcp
# Or use the official MCP Python SDK from Anthropic
# mcp>=0.1.0

# MCP (Model Context Protocol) - must be >=1.11.0 for gds-agent compatibility
mcp[cli]>=1.11.0
# Graph Data Science - must be >=1.16 for gds-agent compatibility
graphdatascience>=1.16
neo4j>=5.28.1

# Documentation toolchain
mkdocs>=1.6
mkdocs-material>=9.5
mkdocs-mermaid2-plugin>=1.0.7
mkdocs-glightbox>=0.3.6
pymdown-extensions>=10.8

# Cypher Query Validation
# NOTE: CyVer requires Python >= 3.10
# Install from GitLab: pip install git+https://gitlab.com/netmode/CyVer.git
# If Python < 3.10, the read-only validation will still work, but CyVer validation will be skipped
CyVer>=2.0.0